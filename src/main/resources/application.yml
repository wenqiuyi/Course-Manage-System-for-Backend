spring:
  application:
    name: TokenDemo
  datasource:
    url: jdbc:mysql://193.112.246.119:3306/gwsj
    username: gwsj
    password: gwsjgwsj
    driver-class-name: com.mysql.cj.jdbc.Driver
  # 保留 JPA 配置（默认即可，如需 JPA 自动建表可添加 spring.jpa.hibernate.ddl-auto: update）
  # jpa:
  #   hibernate:
  #     ddl-auto: update  # 按需开启，JPA 自动建表
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    serialization:
      write-dates-as-timestamps: false

file:
  upload:
    # 相对路径：./ 代表项目启动的根目录（每个成员的项目目录不同，但自动适配）
    path: ./upload


server:
  port: 9999

logging:
  level:
    org.springframework.security: DEBUG

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true  # 开启驼峰转下划线
